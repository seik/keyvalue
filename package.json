{
  "name": "bot",
  "description": "",
  "version": "0.1.0",
  "dependencies": {},
  "devDependencies": {
    "serverless-python-requirements": "^5.1.0"
  },
  "scripts": {
    "deploy": "npx serverless deploy --stage prod"
  }
}
